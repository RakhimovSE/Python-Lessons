БАЗОВЫЕ ТИПЫ ПЕРЕМЕННЫХ
1. Целочисленные (int). 42
2. Дробные / вещественные / числа с плавающей точкой (float / double). Обязательно писать через "."! 18.68
3. Символьные (char). 'h'
4. Строковые (str, string). 'Hello world'
5. Логические (bool, boolean). True/False

Важно задавать типы переменных!
18 + 20 = 38
'18' + '20' = '1820'

ЦИКЛЫ
1. Цикл с параметром / счетчиком
    for <счетчик> in <диапазон>

    for i in range(5, 10) # создаем цикл от 5 ВКЛЮЧИТЕЛЬНО до 10 НЕ ВКЛЮЧИТЕЛЬНО


АЛГОРИТМ подсчета суммы элементов массива
1. Создаем переменной sum,
2. Проходим по всем элементам массива days
3. Добавляем каждый элемент массива days к переменной sum
4. Выводим sum после цикла


DRY - Don't Repeat Yourself